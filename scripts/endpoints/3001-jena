#! /usr/bin/bash

# Ensuring our working directory is the script's location
SCRIPT_LOCATION="$(dirname "$(readlink -f "$0")")"
cd "${SCRIPT_LOCATION}" || exit 1
BENCHMARK_ROOT="$(realpath "${SCRIPT_LOCATION}/../..")"

source config.sh

# Becomes available using the following URL:
# localhost:3001/sparql

ENDPOINT_PORT=3001
ENDPOINT_JAR="${BENCHMARK_ROOT}/bin/jena-endpoint.jar"

source common.sh

# `sparql` is the endpoint path
FLAGS="--port ${ENDPOINT_PORT} sparql"

if [[ "${ENDPOINT_DATA}" != "" ]]; then
	FLAGS="--file ${ENDPOINT_DATA} ${FLAGS}"
else
	FLAGS="--mem ${FLAGS}"
fi

"${JAVA_BIN}" ${JAVA_FLAGS} -jar ${ENDPOINT_JAR} ${FLAGS} &

# (
# setsid "${JAVA_BIN}" ${JAVA_FLAGS} -jar ${ENDPOINT_JAR} ${FLAGS}
# ) &

on_endpoint_ready "http://localhost:${ENDPOINT_PORT}/sparql"

# We launched it, so we stop it too
# PID=$!
# kill -- -$PID
kill $!

# Giving it a second to shutdown, as we're following it up with an (attempted) sig kill
sleep 1

# If it didn't stop, we force it
if ps -p $! > /dev/null; then
	echo "Endpoint did not stop gracefully, forcing shutdown and waiting for PID $!..."
	kill -SIGKILL $!
	wait $!
else
	echo "Endpoint stopped gracefully."
fi

# Cleaning up some of the data jena tends to leave behind
rm -rf "${SCRIPT_LOCATION}/run"
